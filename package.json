{
  "name": "@pi-extensions/lmstudio-models",
  "version": "1.0.0",
  "description": "Automatically fetch and register LM Studio models from EP /v1/models on startup",
  "type": "module",
  "main": "./index.ts",
  "scripts": {
    "lint": "oxlint --fix",
    "format": "oxfmt --check",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "keywords": ["pi-package", "lm-studio", "model-sync"],
  "dependencies": {
    "@mariozechner/pi-coding-agent": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "oxc-language-server": "^1.7.0",
    "oxfmt": "^0.5.0",
    "typescript": "^5.0.0",
    "vitest": "^2.0.0"
  },
  "pi": {
    "extensions": ["./index.ts"]
  }
}
